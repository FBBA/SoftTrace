@model SATrackingDomain.AdditionalToolsDTO

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>AdditionalToolsDTO</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.AdditionalToolName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.AdditionalToolName)
            @Html.ValidationMessageFor(model => model.AdditionalToolName)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
