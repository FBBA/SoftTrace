@model IEnumerable<SATrackingDAL.vwAllSkill>

@{
    ViewBag.Title = "All";
WebGrid grid = new WebGrid(Model, rowsPerPage: 20, ajaxUpdateContainerId: "gridContent");
}

<h2>All</h2>

<div id="gridContent">

@grid.GetHtml(
    tableStyle: "gridTable",
    headerStyle: "gridHead",
    footerStyle: "gridFooter",
    rowStyle: "gridRow",
    alternatingRowStyle: "gridAltRow",
    mode:WebGridPagerModes.All,
    firstText: "<< First", 
    previousText: "< Previous", nextText: "Next >", 
    lastText: "Last >>", 
    columns: new []{
    grid.Column("SkillName", "Skill"),
    grid.Column("SkillCount", "Usage",
    format: @<text>@Html.ActionLink(Html.Encode((string)(item.SkillCount.ToString() + " Application(s)")), "Index", "Application", new { param = item.SkillName, src="sk"}, null)</text>) 
})

</div>