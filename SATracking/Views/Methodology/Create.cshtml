@model SATrackingDomain.ApplicationMethodologyDTO

@{
    ViewBag.Title = "Add Methodology";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ApplicationMethodology</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ApplicationMethodologyText)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ApplicationMethodologyText)
            @Html.ValidationMessageFor(model => model.ApplicationMethodologyText)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
